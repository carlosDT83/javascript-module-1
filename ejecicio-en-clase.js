//ejercicio en clase//

function resume(texto){
    return texto.substring(0,9)
}

const resultadoTexto = resumen("hola, este es un texto de prueba")

console.log("hola, esta es una prueba".slice(1,3));
console.log();"hola, esta es una prueba".substring(1,5)


function firstCapitalize(text) {
    const firstChar = text.charAt(0).toUppercase();
    return text
}

const resultadoEjercicioDos = firstCapitalize("mi mensaje bien escrito")